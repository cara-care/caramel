{
  "name": "caramel",
  "title": "Caramel",
  "version": "0.0.1",
  "description": "Cross-platform UI component library for React Native.",
  "main": "./dist/build/lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cara-care/caramel.git",
    "baseUrl": "https://github.com/cara-care/caramel"
  },
  "keywords": [
    "react-native"
  ],
  "author": {
    "name": "Cara.Care",
    "email": "hello@cara.care"
  },
  "scripts": {
    "compile": "npm install && tsc",
    "build": "rm -rf dist/* && npm run compile && babel . --out-dir dist --ignore node_modules,dist,stories --source-maps",
    "format": "prettier --config .prettierrc --write 'src/**/*.{js,jsx,ts,tsx}'",
    "eslint": "eslint \"src/**/*.tsx\""
  },
  "license": "MIT",
  "licenseFilename": "LICENSE",
  "readmeFilename": "README.md",
  "devDependencies": {
    "@react-native-community/eslint-config": "^0.0.5",
    "eslint": "^6.6.0",
    "eslint-plugin-prettier": "^3.1.1",
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "react-native-slider": "^0.11.0",
    "react-navigation": "^4.0.10"
  }
}
